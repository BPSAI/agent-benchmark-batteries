{
  "difficulty": "medium",
  "expected_shape": {
    "json_schema_keys": [],
    "min_length": 250,
    "must_contain": [
      "def has_conflict",
      "ignore_booking_id",
      "test_has_conflict_ignores_specified_booking_id"
    ],
    "must_not_contain": [
      "TODO"
    ],
    "produces_code": true,
    "required_sections": []
  },
  "id": "v3-room-booking-conflict--short",
  "input_prompt": "`room_booking.py` has `make_calendar()`, `book_room(calendar, room_id, start, end, booking_id)` (adds a half-open `[start, end)` interval booking with no conflict check), `bookings_for_room(calendar, room_id)`, and `cancel_booking(calendar, booking_id)`. Implement a NEW function `has_conflict(calendar, room_id, start, end, *, ignore_booking_id=None)` that returns True if `[start, end)` overlaps any EXISTING booking on `room_id`'s calendar (half-open interval overlap -- back-to-back bookings do not conflict), excluding the booking whose id equals `ignore_booking_id` if given (used when re-checking a booking against itself during a reschedule). Output the full module (existing functions pasted unchanged) plus tests. Include a regression test named exactly `test_has_conflict_ignores_specified_booking_id` proving that passing `ignore_booking_id` equal to an overlapping booking's id makes `has_conflict` return False for that same interval.",
  "metadata": {
    "base_task_id": "v3-room-booking-conflict",
    "format_axes": {
      "framing": "n/a",
      "structure": "short"
    },
    "format_variant": "short",
    "module_name": "room_booking.py",
    "provenance": "original-authored",
    "public_release": true,
    "source": "authored-fixture-set-v3-2026-07"
  },
  "system_prompt": "You are a Driver agent. Output a single Python file: the corrected or extended implementation followed by pytest tests. Follow every requirement in the prompt literally, including exact function names, helper reuse requirements, and the exact test names requested.",
  "task_type": "feature"
}
