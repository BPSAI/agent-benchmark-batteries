{
  "difficulty": "easy",
  "expected_shape": {
    "json_schema_keys": [],
    "min_length": 150,
    "must_contain": [
      "def compute_shipping_cost",
      "test_compute_shipping_cost_is_case_insensitive"
    ],
    "must_not_contain": [
      "TODO"
    ],
    "produces_code": true,
    "required_sections": []
  },
  "id": "v3-zone-shipping-cost--short",
  "input_prompt": "`compute_shipping_cost(weight_kg, zone)` in `shipping_cost.py` calls `zone_rate(zone)`, a dict lookup keyed by LOWERCASE zone name (e.g. `{\"local\": 2.5, \"regional\": 4.0, ...}`). It passes `zone` straight through without normalizing case, so callers passing mixed-case zone names like `\"Local\"` get a `KeyError` instead of the correct rate. Fix `compute_shipping_cost` to lowercase `zone` before the lookup, and raise a `ValueError` with a clear message (instead of letting `KeyError` propagate) when the normalized zone isn't in the rate table. Add a regression test named exactly `test_compute_shipping_cost_is_case_insensitive` proving a mixed-case zone (e.g. `\"Local\"`) now returns the same cost as the lowercase form (`\"local\"`).",
  "metadata": {
    "base_task_id": "v3-zone-shipping-cost",
    "format_axes": {
      "framing": "n/a",
      "structure": "short"
    },
    "format_variant": "short",
    "module_name": "shipping_cost.py",
    "provenance": "original-authored",
    "public_release": true,
    "source": "authored-fixture-set-v3-2026-07"
  },
  "system_prompt": "You are a Driver agent. Output a single Python file: the corrected or extended implementation followed by pytest tests. Follow every requirement in the prompt literally, including exact function names, helper reuse requirements, and the exact test names requested.",
  "task_type": "bugfix"
}
